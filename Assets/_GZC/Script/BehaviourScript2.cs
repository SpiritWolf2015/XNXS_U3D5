using UnityEngine;
using System.Collections;

public class BehaviourScript2 : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        Vector3 v3 = new Vector3( );
        v3 = Vector3.zero;
        //GameObject go = new GameObject( );
        //Texture t = new Texture( );
        //Destroy(t);
        //t = null;
	}
}
